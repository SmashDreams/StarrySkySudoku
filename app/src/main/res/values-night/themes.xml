<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- 应用基础主题。 -->
    <style name="Base.Theme.StarrySkySudoku" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 主品牌色可在这里配置。 -->

        <!-- 在这里扩展通用主题配置。 -->
    </style>

    <style name="Theme.StarrySkySudoku.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.StarrySkySudoku</item>
    </style>
</resources>
